import 'package:flutter/material.dart';

class RiwayatMedisPage extends StatelessWidget {
  const RiwayatMedisPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Data Riwayat Pemain"),
    );
  }
}
